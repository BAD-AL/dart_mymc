name: dart_mymc
description: PS2 Memory Card manager — Dart port of mymc. Read/write .ps2 card images and .psu/.max/.sps/.cbs save files.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.0

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  archive: ^3.6.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
